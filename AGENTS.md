# AGENTS.md — Card Duel

## 道：项目哲学

### 1. 通用大于特判

先判断若干需求是否共享同一语义，优先扩展一处共同机制。真实差异应当清楚保留；缺少共同需求时，不要提前建立万能抽象。

- 正例：多个入口需要完成同一件事，由一套共同机制处理，各入口只提供自身的数据。
- 反例：每个入口各写一套相似逻辑，后续修复时再逐处复制修改。

### 2. 从第一性原理出发

暂时放下当前实现和既有结论，从原始需求重新推导。确认用户希望得到什么、哪些行为已经成为外部契约、现有判断有多少证据，以及哪些条件必须长期成立。完成这些确认后，再选择具体技术。

- 正例：先定义成功结果和不可接受的后果，通过证据找到原因，最后选择改动位置。
- 反例：从熟悉的技术或最初的猜测出发，直接修改表面症状并扩大改动范围。

### 3. Fail fast，避免静默降级

约束已经不成立，或者结果无法确认时，及时停止相关操作，并把错误和必要上下文交给调用者。静默降级、吞掉错误或猜测默认值，会让一次清楚的故障变成更晚出现的不一致。

- 正例：已知的可恢复情况返回明确结果；状态无法判断时直接报错，并保留诊断信息。
- 反例：捕获所有异常后改走默认路径，最终仍向调用者报告成功。

### 4. 可维护性优先

软件工程没有魔法。今天省下的理解成本，往往会在后续修改中以更高代价回来。可维护性包括清楚的命名和责任、单一的事实来源、明确的边界，以及可以验证的行为。

- 正例：选择容易理解和验证的结构，接受已经评估过的少量性能开销。
- 反例：为了未经测量的局部收益，引入平行实现、隐藏状态或依赖时序的行为。

### 5. KISS 与 60/95 原则

选择能够完整解决当前问题的最简单方案。60/95 表示用约 60% 的复杂度覆盖约 95% 的现实边界；缺少现实依据的罕见场景可以留待真正出现时处理。

- 正例：先完整解决常见且影响较大的场景，写清当前边界，等真实需求出现后再扩展。
- 反例：为想象中的极端情况进行层层兜底，或者选择复杂度更多的实现，长期承担很少产生价值的复杂度。

### 6. 简洁与优雅

概念应当清楚，责任应当放在拥有相关事实的地方，数据流和错误路径应当容易追踪。实现应尊重用户选择和既有生态。

- 正例：解决方案的责任归属清楚，数据和错误沿一条容易说明的路径流动。
- 反例：各层不断补偿上一层的特殊情况，每段代码都能单独解释，组合后的行为却难以说明。

### 7. 遵循，但不拘泥

原则之间可能产生张力。遇到冲突时，回到原始目标和已有证据，说明取舍，运用第一性原理和实践判断选择当前合适的方案；新事实推翻旧判断时，应当修改设计。

- 正例：事实发生变化后重新评估方案，记录理由，并尊重用户已经明确作出的选择。
- 反例：机械引用某条原则结束讨论，或为了保持原有设计而忽略新的事实。

---

## 术：工程实践

### 核心架构

- **单一规则源**：`game.rs` 是所有战斗规则、效果结算、CPU 决策的唯一事实来源。编译为原生（服务端/CLI/Tauri）和 WASM（浏览器单机）。
- **数据驱动**：禁止在任何引擎代码中硬编码卡牌属性、效果数值、规则常量。所有数据来自 `app/card_duel/data/*.json`。
- **入口分离**：共享逻辑放在 `server.rs`（HTTP）与 `game.rs`（逻辑）。`bin/server.rs` 与 `main.rs` 只做启动与参数解析，禁止直接实现业务逻辑。

### 模块边界

| 模块 | 职责 | 禁止 |
|------|------|------|
| `core.js` / `engine.js` | 引擎门面，通过 WASM 调用 `game.rs` | 操作 DOM、发起网络请求、读写 `localStorage` |
| `battle.js` | 战斗 UI 与动画编排 | 直接修改游戏状态（必须通过 `core.js` API） |
| `game.rs` | 纯游戏逻辑、AI、数据加载 | 引入 Tauri API、HTTP 处理、WebView 操作 |
| `server.rs` | HTTP 服务、API 路由 | 直接操作游戏状态（必须通过 `game.rs` API） |

### 数据契约

- 修改 JSON schema 时，必须检查 JS（`data.js`）与 Rust（`game.rs`）的解析兼容性。
- 若破坏旧数据兼容（如 localStorage `v3` → `v4`），必须保留并更新迁移逻辑。
- 游戏数据文件路径（相对于项目根）：`app/card_duel/data/`。

### 路径与资源

- 文件路径解析必须使用 `web_root()`，禁止依赖 CWD，确保双击 exe 可直接运行。
- 音频 BGM 格式：OGG Vorbis 192kbps，无元数据。转换命令：`ffmpeg -i input.mp3 -map_metadata -1 -c:a libvorbis -b:a 192k output.ogg`
- JS `import` 保留 `?v=__VERSION__` 后缀，由 `server.rs` 在 `index.html` 中替换为 `CARGO_PKG_VERSION`。Node.js 直接 require 前需先剥离此后缀。

### 术语与命名

- 统一使用 `subfaction`；禁止引入 `role`。
- `localStorage` 键名为 `cardgame_db_v4`。

### 错误处理

- 禁止静默吞掉错误。Rust 侧使用 `Result` 传播；JS 侧至少 `console.error`。
- 游戏状态无法确认时，停止操作并返回错误，禁止猜测默认值继续游戏。

---

## 参考文档

项目进度、债务状态、构建命令等会变化的内容归档在 `docs/` 下：

| 文档 | 内容 |
|------|------|
| `docs/KnownConstraints.md` | 债务、验证缺口、迁移状态等已知约束的现状与应对 |
| `docs/BuildAndRun.md` | 本地构建、CI 构建、运行、gh CLI 查日志、修改规则后的验证流程 |
| `docs/ProjectOverview.md` | 项目概况、技术栈、目录结构 |
| `docs/BackendStructure.md` | Rust 模块职责与数据流 |
| `docs/FrontendGuide.md` | JS 模块职责与依赖关系 |
| `docs/EngineSyncContract.md` | WASM 绑定接口契约 |